
.PHONY: build test clean

build:
	@echo "Building the project..."
	# Add your build commands here, e.g., gcc -o myapp main.c

test:
	@echo "Running tests..."
	# Add your test commands here, e.g., ./myapp --test

clean:
	@echo "Cleaning up..."
	# Add your clean commands here, e.g., rm -f myapp *.o
